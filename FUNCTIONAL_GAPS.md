# Auditoria Funcional — GestCondo

Data: 2026-07-06, **atualizada 2026-07-24, 2026-07-25** (Fase A.2 promovida a produção; Fase B — balanço patrimonial e quotas extraordinárias — implementada em desenvolvimento). Legenda: ✅ Implementado · 🟡 Parcial/básico · ❌ Em falta. Prioridade pensada para o objetivo declarado (produto profissional multi-condomínio para o mercado português): **P0** bloqueador estrutural, **P1** necessário para MVP utilizável, **P2** necessário para venda comercial, **P3** diferenciador/avançado.

## Visão geral honesta

O que existia à data desta auditoria (2026-07-06) cobria uma fatia pequena e bem construída de **gestão administrativa básica de um único condomínio**: finanças simples, avisos, ocorrências, frações, condóminos com fluxo de aprovação. **A entidade `condominio` e o isolamento multi-tenant foram entretanto implementados** — schema e queries em 2026-07-06, onboarding por código de convite em 2026-07-22 (ver secção 1 e `SECURITY_AUDIT.md` S9). A reconstrução multi-tenant que esta auditoria antecipava como pré-requisito já foi feita, não está por fazer.

Módulo inteiro pedido como essencial para o mercado português — **Assembleias** — ganhou o seu núcleo P1 em 2026-07-09 (convocatória, ordem de trabalhos, presenças/procurações, quórum e votação por permilagem, ata imutável) — ver secção 2. **Gestão financeira formal** (orçamentos, dívidas por fração, recibos, exportação, seguro obrigatório, fundo de reserva) ganhou uma primeira versão em 2026-07-07/08 — ver secção 3 — geração automática de quotas por rateio de permilagem (com isenção de elevador), juros de mora, reconciliação bancária (importação CSV + conciliação manual assistida) e relatório de movimentos em PDF ficaram resolvidos em 2026-07-21; falta só a exportação `.xlsx` real (hoje é CSV), se vier a ser necessária.

**Verificado em runtime em 2026-07-21**, contra a base de dados Neon real do utilizador (não só revisto em código): migrações `0007_assembleias`/`0008_upload-ficheiros` aplicadas; ciclo completo de uma assembleia (convocar → adicionar ponto → votar → aprovar deliberação → marcar realizada → aprovar ata → confirmar imutabilidade → imprimir ata) testado manualmente no browser; upload real de ficheiro testado nos três pontos que o usam (documento, foto de ocorrência, apólice de seguro), incluindo a eliminação em cascata do ficheiro no Vercel Blob. Nesse teste encontrou-se e corrigiu-se um desfasamento entre o limite de corpo das server actions (10MB) e o limite real de ficheiro documentado (15MB) — `next.config.mjs`, `bodySizeLimit` subido para 16MB.

---

## 1. Gestão do condomínio

| Funcionalidade | Estado | Nota | Prioridade |
|---|---|---|---|
| Condomínios/edifícios (entidade própria) | ✅ Implementado 2026-07-22 | Tabela `condominio` (nome, morada, NIF) com isolamento por `condominioId` em todas as queries (ver `SECURITY_AUDIT.md` S9). Ecrã de definições em `/condominio` (editar nome/morada/NIF, gerar código de convite). Onboarding: conta sem `membro` escolhe entre entrar com código de convite (fica pendente, como qualquer registo novo) ou criar um condomínio novo (fica admin). Testado em runtime com duas contas descartáveis e dois condomínios distintos. | — |
| Frações autónomas | 🟡 Parcial | `app/(app)/fracoes/page.tsx` + schema `fracao`: identificação, proprietário (texto livre), permilagem, contactos, notas. **2026-07-23**: campos `letra` (código curto, ex. "A", alinhado com a notação usada nos mapas de administrações externas como a MBD Gest) e `tipoTitular` (proprietário/inquilino/usufrutuário/locatário/antigo), ambos opcionais, editáveis em `EditarFracaoDialog`. Ligada ao condomínio por `fracao.condominioId` desde 2026-07-06. O que falta tem linha própria abaixo: proprietário relacional, histórico de titularidade e área em m². | P2 (completar) |
| Permilagens | 🟡 Parcial — aviso adicionado 2026-07-21 | Campo `permilagem` existe e soma-se no dashboard. Ainda não há validação bloqueante de que a soma é 1000‰, mas o diálogo de geração de quotas (`components/financas/gerar-quotas-dialog.tsx`) mostra sempre a permilagem total apurada, com aviso visual se desviar de 1000‰ — o rateio usa sempre a soma real, nunca um total fixo, por isso continua matematicamente correto mesmo que a soma não seja 1000‰. | P1 → P2 |
| Proprietários | ✅ Compropriedade e NIF resolvidos 2026-07-21 | `fracao.proprietario` continua texto livre (registo legal de propriedade, válido mesmo antes de o proprietário ter conta na app), agora com `fracao.nif` opcional, mostrado em `/fracoes` sob o nome do proprietário. **Desde 2026-07-07**, um `membro` com `perfil: condomino` pode ligar-se de facto a essa fração via `membro.fracaoId`. **Compropriedade** (mais do que um `membro` condomino na mesma fração) já era tecnicamente possível na BD, mas sem UI que o mostrasse — `/fracoes` tem agora a coluna "Condómino(s) com conta" (só visível a quem gere/audita), listando todos os condóminos com conta ligados a cada fração. Edição de frações já existentes **resolvida 2026-07-23** (`components/fracoes/editar-fracao-dialog.tsx` + `atualizarFracao`): identificação, proprietário, permilagem, `nif`, letra e tipo de titular passaram a ser corrigíveis depois da criação. | — |
| Inquilinos | 🟡 Parcial — implementado 2026-07-07 | `perfil: inquilino` (desde o redesenho de papéis) agora pode ligar-se a uma fração via `membro.fracaoId`, tal como um condómino — um proprietário e o seu inquilino podem ter contas em simultâneo, ambas ligadas à mesma fração, cada uma com o seu nível de acesso (inquilino não vê finanças/frações, ver `SECURITY_AUDIT.md` S8). Editável em `/condominos` (`EditarMembroDialog`, agora com um seletor de fração em vez de texto livre). | P1 → P2 |
| Contactos de emergência | ✅ **Resolvido, verificado em dev e em produção desde 2026-07-26** | Tabela `contacto_emergencia` (nome, telefone, descrição opcional), distinta de `fornecedor` (que é gestão comercial/financeira) — visível a qualquer membro aprovado, gerida só por admin/gestor. Vive como um `Card` no Painel (`/`), não em `/condominio` (essa página é só para quem gere, e a lista tem de ficar visível a todos numa emergência). Sem página nova nem item na barra lateral. Testado em runtime: adicionar, mostrar (telefone como link `tel:`), eliminar. Migração `0035` aplicada a produção (snapshot criado antes, `db:check-drift` OK, totais de `condominio`/`movimento` inalterados). | — |
| Representantes legais | ✅ **Resolvido, verificado em dev 2026-07-26** | Duas colunas novas em `fracao` (`representanteLegal`, `representanteLegalContacto`), mesmo padrão de `nif`/contactos — texto livre, um só representante por fração, dentro de "Mais opções" em `nova-fracao-dialog.tsx`/`editar-fracao-dialog.tsx`. Mostrado em `/fracoes` sob o proprietário, só a quem já vê contactos. Não altera as minutas de convocatória/procuração/interpelação (continuam a usar só `proprietario`). Migração `0037` aplicada a dev; produção pendente de autorização. | — |
| Histórico de titularidade | ❌ Em falta | Alterar `fracao.proprietario` sobrescreve o valor anterior sem histórico — perde-se o registo de quem era o proprietário antes de uma venda, relevante para apurar responsabilidade por dívidas. | P2 |
| Sucessão do titular (óbito) | ❌ Em falta — registado 2026-07-26 | Sem fluxo dedicado para quando o único proprietário de uma fração falece e os herdeiros/cabeça-de-casal não têm acesso à conta antiga. Hoje o herdeiro pode criar conta nova (código de convite) e o admin liga-a à fração (`fracaoId`), mas **não há forma de remover/desativar a conta antiga em `/condominos`** — `components/condominos/membro-status-actions.tsx` só liga `rejeitarMembro` a pedidos *pendentes*, uma conta já aprovada só tem "Editar" (nome/fração/telefone), nunca eliminar. A conta do falecido fica visível para sempre, incluindo duplicada na coluna "Condómino(s) com conta" de `/fracoes`. | P2 |
| Documentos do prédio | 🟡 Parcial | `app/(app)/documentos/page.tsx`, com upload real de ficheiro desde 2026-07-09 (`documento.url`/`nomeFicheiro`, store privado — ver secção 6). Falta permissões por documento, versionamento e campos próprios por tipo. | P2 |
| Regulamento do condomínio | ✅ **Resolvido e verificado em dev, 2026-07-26** | Continua a viver como categoria de documento (`categoria: "regulamento"`), mas ganhou confirmação de leitura — reaproveita por completo o mecanismo já existente para avisos/convocatórias (`lib/confirmacao-leitura.ts`, tabela `confirmacao_leitura`, sem migração nova: `entidade` já era `text` livre, só ganhou o valor `'documento'`). Aplicado a **todos** os documentos, não só regulamentos (mesmo critério de avisos, que também não distinguem por tipo). Testado em runtime: confirmar, contagem, lista de quem confirmou. | — |
| Seguro obrigatório | ✅ Implementado 2026-07-08 | Entidade `seguro` própria (seguradora, apólice, tipo, validade, prémio), gerida em `/financas` (separador "Seguro"), com aviso visual quando a apólice expirou ou expira nos próximos 30 dias. Anexo da apólice em PDF resolvido 2026-07-09 (Vercel Blob). **2026-07-23**: cada apólice pode agora indicar as frações que cobre além do edifício (tabela `seguro_fracao`, checkboxes no diálogo de criação, coluna "Frações" em `/financas` e coluna "Seguro" em `/fracoes`). | — |
| Capital seguro (valor de reconstrução atualizado, DL 268/94) | ✅ Implementado 2026-07-22 | Distinto do prémio: é o valor pelo qual o edifício está seguro, obrigatório atualizar anualmente. Gap identificado por comparação com um template de contabilidade de condomínio real (`14_Seguro_Actualizacao`). Campo `seguro.capitalSeguro`, opcional, mostrado em `/financas` junto do prémio. | — |
| Fundo comum de reserva | ✅ Implementado 2026-07-08 | Movimentos podem ser marcados com `destino: "reserva"` em vez de `"geral"` e passam a ser seguidos numa conta própria (`getSaldoFundoReserva()`), visível no dashboard e em `/financas`, excluída do saldo da conta corrente normal. Falta ainda impor por regra a % mínima de quota destinada ao fundo (DL nº 268/94, art. 4º — 10% da quota-parte anual) — hoje a segregação é manual, por lançamento. **Prioridade revista 2026-07-23** — é obrigação legal, não só conveniência; proposta de sugerir 10% por omissão (editável) ao gerar quotas, ver `docs/product/MBD_GEST_GAP_ANALYSIS.md` (G03). | P1 (sugerir automaticamente o mínimo legal) |
| Dados formais do edifício | ✅ **Resolvido, verificado em dev e em produção desde 2026-07-26** | 5 campos novos em `condominio` (número matricial, conservatória do registo predial, licença de habitação, projeto/arquiteto, área de construção em m²), todos opcionais e em texto livre (tal como `morada`/`nif` já eram) — editáveis em `/condominio`, mesmo formulário e botão já usados para nome/morada/NIF. Sem tabela nova. Testado em runtime: preencher, persistir após recarregar, e gravar `null` corretamente quando deixado em branco. Migração `0034` aplicada a produção (snapshot criado antes, `db:check-drift` OK, totais de `condominio`/`movimento` inalterados). | — |
| Património/inventário do condomínio | ✅ **Resolvido, verificado em dev e em produção desde 2026-07-26** | Tabela `patrimonio` (nome, categoria, data de aquisição, valor de aquisição, valor atual, notas) — informação administrativa, sem lançar movimentos nem alterar saldos. Valor atual indicado manualmente, sem fórmula automática de depreciação (mantido simples de propósito). Visível a admin/gestor/auditor em `/condominio` (mesmo critério de acesso da restante página). Testado em runtime: adicionar (com data/valores), mostrar formatado, eliminar. Migração `0036` aplicada a produção (snapshot criado antes, `db:check-drift` OK, totais de `condominio`/`movimento` inalterados). | — |
| Área privativa/comum por fração (m²) | ✅ **Resolvido, verificado em dev e em produção desde 2026-07-26** | Duas colunas novas em `fracao` (`areaPrivativa`, `areaComum`), ambas opcionais, mesmo padrão de `nif`/`representanteLegal` — dentro de "Mais opções" em `nova-fracao-dialog.tsx`/`editar-fracao-dialog.tsx`. Mostrado em `/fracoes` sob a identificação (não sob o proprietário, por ser um atributo físico da fração, não do titular), visível a todos com acesso financeiro (não é dado pessoal). Sem cálculo automático — a área comum imputada não se deriva da permilagem, mantido como dois campos livres. Migração `0039` aplicada a produção (snapshot criado antes, `db:check-drift` OK, totais de `condominio`/`movimento` inalterados). | — |
| Exportação/importação completa dos dados do condomínio | ✅ **Resolvido 2026-07-26, em produção desde 2026-07-26** | `exportarCondominio()`/`importarCondominio()` (`app/actions/condominio.ts`) — exportação em JSON de todas as tabelas do condomínio (admin, botão em `/condominio`); importação só cria um condomínio **novo** (terceiro modo no onboarding), nunca escreve sobre um existente, com todos os ids remapeados. Não inclui conteúdo binário de anexos nem a lista de condóminos com conta (`membro`) — ver comentário em `exportarCondominio` para o porquê. Testado com `pnpm test`/`pnpm test:db`. | — |
| Ajuda/manual dentro da aplicação | ✅ **Resolvido 2026-07-26, em produção desde 2026-07-26** | Página `/ajuda`, visível a todos os perfis, com uma secção por módulo escrita para um utilizador sem experiência nenhuma (linguagem simples, passo a passo, cabeçalhos semânticos para leitor de ecrã). Teste real com NVDA continua pendente (ver `docs/audit/ACCESSIBILITY_AUDIT.md`). | — |
| Controlo de acesso por subscrição (proteção contra uso sem pagamento) | ✅ **Resolvido 2026-07-26, em produção desde 2026-07-26** | Painel `/plataforma`, novo papel `user.operadorPlataforma` (distinto do `superAdmin`, reservado para a futura empresa gestora — ver secção 8/Fase 5 do `ROADMAP.md`). Suspender bloqueia `requireMembroAprovado`/`requireAdmin` para todo o condomínio, incluindo administradores. Cobrança em si continua manual. Só `rjc-si@netcabo.pt` é operador em produção (2026-07-26) — `rui.coelho@netcabo.pt` teve o acesso removido, em dev e em produção, por já não ser necessário; conta de operador sem `membro` nenhum é redirecionada diretamente para `/plataforma` em vez de onboarding. | — |
| MFA obrigatório para `operadorPlataforma` | ✅ **Resolvido e verificado em produção, 2026-07-26** | `requireOperadorPlataforma()` (`lib/session.ts`) passa a exigir também `user.twoFactorEnabled`, bloqueando `listarCondominiosPlataforma()`/`alterarEstadoSubscricao()` enquanto o MFA não estiver ativo. A página `/plataforma` mostra `MfaObrigatorioScreen` (reutiliza `<MfaSection />`, já usada em `/os-meus-dados`, sem depender de `membro`) em vez da tabela até a conta ativar o MFA. Fluxo de ativação (QR code + confirmação) testado e confirmado pelo Rui em produção. | — |
| Gerir operadores da plataforma a partir de `/plataforma` | 🟡 **Implementado 2026-07-26, verificação em runtime pendente** | Secção "Operadores da plataforma" em `/plataforma` (`listarOperadoresPlataforma()`/`promoverOperadorPlataforma()` em `app/actions/plataforma.ts`) — lista os operadores atuais (email + estado do MFA) e permite promover **um utilizador já existente** (por email). Só permite adicionar, nunca remover/despromover — remoção continua manual, por decisão já registada (evita o risco de um operador se auto-remover ou remover o último operador). Sem entrada em `audit_log` (essa tabela exige `condominioId`, que esta ação não tem) — fica só `console.log` no servidor. Testes automáticos (`pnpm test`/`tsc`/`eslint`) passam; falta confirmação em runtime pelo Rui (não posso testar por depender de sessão iniciada). | — |

## 2. Assembleias — implementado 2026-07-09 (núcleo P1), verificado em runtime 2026-07-21

| Funcionalidade | Estado | Prioridade |
|---|---|---|
| Convocatórias | ✅ Tipo (ordinária/extraordinária), local, 1ª e 2ª convocatória (`app/actions/assembleias.ts:criarAssembleia`). **2026-07-23**: minuta formal imprimível (`/assembleias/[id]/convocatoria`) com as menções obrigatórias do art. 1432.º CC — datas/local/ordem de trabalhos, identificação dos assuntos que exigem unanimidade (novo campo `assembleia_ponto.exigeUnanimidade`, art. 1432.º/4), notas de quórum e aviso no ecrã quando a antecedência legal de 10 dias já não é cumprível; notas legais verificadas contra o texto integral do art. 1432.º em 2026-07-23 (corrigida a regra da unanimidade do n.º 8 e acrescentados os n.os 9–11). Minuta de procuração imprimível (`/assembleias/[id]/procuracao`, art. 1431.º/3), preenchível à mão — os dados pessoais do representante ficam fora da app. Carta de interpelação de quotas em atraso (`/financas/interpelacao/[fracaoId]`, arts. 805.º/806.º CC + art. 6.º DL 268/94 verificados na fonte), pré-preenchida com as dívidas da fração e prazo de pagamento selecionável (8/15/30 dias) | P1 |
| Ordem de trabalhos | ✅ Pontos numerados sequencialmente, adicionáveis enquanto a assembleia não está encerrada | P1 |
| Registo de presenças | ✅ Por fração, com nome do representante | P1 |
| Representações/procurações | ✅ Campo `tipo` (`presencial`/`procuracao`) na presença | P1 |
| Cálculo de quórum (por permilagem) | ✅ Permilagem presente / permilagem total do condomínio, mostrado na página de detalhe e na ata. Sem distinção automática de limiar 1ª vs. 2ª convocatória — a app mostra o número, quem qualifica se o quórum exigido foi atingido é o administrador | P1 |
| Votação por permilagem | ✅ Voto por fração (`favor`/`contra`/`abstencao`) por ponto, com soma de permilagem por opção | P1 |
| Deliberações | ✅ Resultado (`aprovado`/`reprovado`/`adiado`) definido manualmente pelo administrador por ponto, com base nos números mostrados | P1 |
| Atas | ✅ **P1** — texto livre + ata gerada automaticamente (presenças, votação, deliberações) em `/assembleias/ata/[id]`, imprimível. Torna-se imutável assim que aprovada (`estado: 'aprovada'` bloqueia qualquer escrita nas tabelas associadas). **Resolvido 2026-07-26, em produção desde 2026-07-26**: numeração sequencial do livro de atas (`assembleia.numero`, `unique(condominioId, numero)`), atribuída na aprovação, mostrada em `/assembleias`, na página de detalhe e na ata | — |
| Anexos à ata | ✅ **Resolvido, verificado em dev e em produção desde 2026-07-26** | Tabela `assembleia_anexo` própria (distinta de `documento`, que é solto sem ligação a uma assembleia), ficheiro obrigatório (PDF/imagem, 15MB), servido via `app/api/ficheiros/route.ts`. Só pode ser adicionado/removido enquanto a assembleia não estiver aprovada/cancelada (mesma imutabilidade da ata) — confirmado em runtime (upload, download, listagem na ata imprimível, controlos escondidos depois de aprovada, eliminação). Migração `0032` aplicada a dev e produção (snapshot criado antes, `db:check-drift` OK, totais de `condominio`/`movimento` inalterados). **Corrigido de passagem**: a mesma rota nunca verificava `documentoFornecedor`, pelo que os anexos de documentos de fornecedor devolviam sempre 404 — corrigido na mesma alteração. | — |
| Histórico de assembleias | ✅ Lista em `/assembleias`, ordenada por data | P1 |
| Videoconferência (quando legalmente admissível) | ❌ | P3 |
| Notificação por email de convocatórias | ✅ Envio automático a todos os membros aprovados do condomínio ao convocar (`lib/email.ts`) | — |
| Registo de receção/leitura/confirmação de convocatória | ✅ **Resolvido 2026-07-26, em produção desde 2026-07-26** — botão "Confirmar leitura" na página de detalhe da assembleia, registado em `confirmacao_leitura` (membro, data/hora), visível a qualquer membro; mesmo mecanismo para avisos (ver secção 5) | — |

Este é o módulo funcionalmente mais crítico para o mercado português (as assembleias e as suas atas são o instrumento legal central da vida do condomínio, Código Civil arts. 1430º–1438º-A). Foi construído de raiz em 2026-07-09, com a exigência de integridade que o justificava: uma ata aprovada não é silenciosamente editável — `estado: 'aprovada'` bloqueia qualquer escrita nas tabelas associadas, verificado em runtime em 2026-07-21. Falta apenas videoconferência (❌ acima); numeração sequencial, confirmação de leitura da convocatória e anexos à ata foram resolvidos em 2026-07-26.

## 3. Gestão financeira

| Funcionalidade | Estado | Nota | Prioridade |
|---|---|---|---|
| Orçamento anual | ✅ Implementado 2026-07-07, geração de quotas 2026-07-21, balanço previsto vs. real 2026-07-22, rubricas por categoria 2026-07-24 (Fase A.2), em produção desde 2026-07-25 | Tabela `orcamento` (ano, valor anual, valor anual do elevador opcional, notas), um por condomínio/ano, gerida em `/financas` (separador "Orçamentos"), com um botão "Gerar quotas mensais" que cria as 12 quotas/fração automaticamente (ver linha "Quotas ordinárias"). **2026-07-24**: rubricas discriminadas por categoria de despesa (`orcamento_rubrica`, "Gerir rubricas" no menu do orçamento) — a soma das rubricas ultrapassar o valor anual gera aviso, não bloqueio. Mergeado em `main` (migração `drizzle/0026_great_sabretooth.sql`, commit `93f605a`), **promovido a produção em 2026-07-25**. | — |
| Balanço orçamento aprovado vs. real, com desvio (art. 1436º CC) | ✅ Implementado 2026-07-22 | `/financas/balanco/[id]`, a partir de um orçamento — compara o valor anual aprovado com receitas/despesas reais lançadas nesse ano (destino "geral", excluindo fundo de reserva), despesas discriminadas por categoria, e desvio (real − previsto) em € e %. Gap identificado por comparação com um template de contabilidade de condomínio real (`6_CONTAS`/`6A_BALANCO`). Página imprimível, mesmo padrão do relatório de movimentos. **2026-07-24**: passa a discriminar por rubrica de orçamento quando existirem (ver linha "Orçamento anual") — sem rubricas definidas, continua a usar o total do orçamento como antes. **Atenção à nomenclatura** (2026-07-23): isto é "orçamento vs. real", **não** um balanço patrimonial contabilístico (Ativo/Passivo/Situação Líquida) — esse continua em falta, ver linha "Balanço patrimonial" abaixo e `docs/product/MBD_GEST_GAP_ANALYSIS.md` (G09). | — |
| Balanço patrimonial (Ativo/Passivo/Situação Líquida) | ✅ Implementado 2026-07-25 (Fase B, G09), em produção desde 2026-07-25 | Distinto do balanço orçamento-vs-real acima. `getBalancoPatrimonial(exercicioId)` (`app/actions/contas-financeiras.ts`) — cálculo puro, sem tabela nova: Ativo = disponibilidades (`calcularSaldoConta` por conta ativa) + dívidas de condóminos por receber; Passivo = saldo não pago de `documento_fornecedor`; Situação Líquida = fundo de reserva + resultados acumulados (resíduo, garante a equação por construção). Separador "Balanço patrimonial" em `/financas` (seletor de exercício) + página imprimível `/financas/balanco-patrimonial/[exercicioId]`. **Limitação assumida**: não inclui adiantamentos de condóminos como passivo próprio (G04 não implementado). Testado com `lib/db/balanco-patrimonial.dbtest.ts` (2 casos: cálculo correto dos 3 blocos, isolamento multi-tenant) e smoke test manual em dev e em produção (dados reais do condomínio piloto: Ativo 7665,94 €, Passivo 0 €, Situação Líquida 7665,94 €, equação verificada). **Não validado contra o PDF de referência da Fase original** (`Exemplo MBD.pdf` não está no repositório) — só a fórmula e a consistência interna foram verificadas. | — |
| Quotas ordinárias | ✅ Rateio automático implementado 2026-07-21 | Um `movimento` do tipo `receita`, **obrigatoriamente ligado a uma fração** (`criarMovimento`, ver `app/actions/financas.ts`). `app/actions/orcamentos.ts:gerarQuotasOrcamento` + `lib/rateio.ts` geram as 12 quotas mensais de cada fração a partir do orçamento anual, rateadas por permilagem — com confirmação prévia (pré-visualização por fração) e bloqueio contra gerar em duplicado para o mesmo orçamento (verificado por `movimento.orcamentoId`, uma FK real). Suporta isenção da parcela do elevador por fração (`fracao.isentaElevador`, comum para o rés-do-chão em Portugal — Código Civil art. 1424º) com um valor anual de elevador próprio no orçamento, rateado só pelas frações não isentas. Lançamento manual um a um continua disponível para casos fora do rateio automático (quotas extraordinárias, ajustes). | — |
| Quotas extraordinárias | ✅ Implementado 2026-07-25 (Fase B, G05), em produção desde 2026-07-25 | `movimento.assembleiaPontoId` (FK simples para `assembleia_ponto`, `onDelete: set null` — sem FK composta, `assembleia_ponto` não tem `condominioId` próprio; isolamento garantido na server action). Ao lançar uma receita, pode ligar-se opcionalmente a um ponto de assembleia já aprovado (de uma assembleia com ata já aprovada) — `getPontosAprovadosParaQuota()`, validação em `criarMovimento`/`atualizarMovimento` (`app/actions/financas.ts`). Badge "Quota extraordinária" na listagem de movimentos. Testado com `lib/db/quota-extraordinaria.dbtest.ts` (4 casos: elegível, cross-tenant rejeitado, ponto não aprovado rejeitado, assembleia não aprovada rejeitada). | — |
| Despesas comuns | 🟡 Parcial | `movimento` tipo `despesa`, categoria livre, sem rateio automático por permilagem. | P1 |
| Documentos de fornecedores com ciclo completo (pagamentos parciais) | ✅ Implementado 2026-07-24 (Fase A.2, peça 1), em produção desde 2026-07-25 | Entidades próprias `documento_fornecedor` + `pagamento_documento_fornecedor` (`lib/db/schema.ts`), substituindo o `pago: boolean` binário do `movimento` — `valorPago`/`saldo` nunca persistidos, sempre calculados a partir da soma real dos pagamentos (`lib/documentos-fornecedor.ts`). Suporta anexo (Vercel Blob privado, mesmo padrão de `seguro`). Novo separador em `/financas`. Testado com `pnpm test`/`pnpm test:db` (`lib/documentos-fornecedor.test.ts`, `lib/db/documentos-fornecedor.dbtest.ts`, incluindo a FK composta a rejeitar um pagamento de outro condomínio). Mergeado em `main` (migração `drizzle/0025_charming_epoch.sql`, commit `cdf9007`), **promovido a produção em 2026-07-25**. | — |
| Adiantamentos de condómino (crédito disponível) | ❌ Em falta | Sem conceito próprio distinto de "pago antecipadamente". Gap identificado 2026-07-23, ver `docs/product/MBD_GEST_GAP_ANALYSIS.md` (G04). | P2 |
| Dossier automático de apoio à assembleia | ❌ Em falta | Consolidação num único PDF (capa, comunicação, resumo financeiro, mapas, balanço, anexos) — todas as peças já existem em separado, nunca foram reunidas. Base legal em arts. 1432º e 1436º/j CC. Análise de impacto RGPD (exposição de dívidas de todos os condóminos) em `docs/product/MBD_GEST_GAP_ANALYSIS.md` (G18) — requer decisão do utilizador antes de implementar. | P2 |
| Fundo comum de reserva | ✅ Implementado 2026-07-08 | Ver secção 1. | — |
| Dívidas por condómino/fração | ✅ Implementado 2026-07-07 | Separador "Dívidas por fração" em `/financas` (`getMapaSaldos()`): para cada fração, quotas lançadas − quotas pagas = dívida. Responde diretamente a "quanto deve o 2ºEsq?". Verificado com um teste de integração real (`lib/db/mapa-saldos.dbtest.ts`) contra o tipo `numeric` do Postgres. | — |
| Juros de mora por atraso | ✅ Implementado 2026-07-21 | `lib/juros.ts` + `app/actions/financas.ts:lancarJurosMora` — juros simples, proporcionais aos dias de atraso, sobre quotas por pagar cuja data já passou (a própria data da quota é usada como vencimento, sem campo de vencimento nem tolerância próprios). A taxa anual é introduzida pelo administrador no momento de lançar (`components/financas/lancar-juros-dialog.tsx`, com pré-visualização por fração) — a app não sugere nem guarda uma taxa, por depender do regulamento do condomínio ou da taxa legal em vigor. Confirmado cria um `movimento` de receita por fração com dívida. Penalizações fixas (não proporcionais a juros) continuam por implementar, se aplicável ao regulamento do condomínio. | P2 (penalizações fixas, se necessário) |
| Recibos | ✅ Implementado 2026-07-07, **acesso self-service e pagador diferente do proprietário resolvidos e em produção desde 2026-07-26** | Página `/financas/recibo/[id]` (só para movimentos tipo receita) com identificação do condomínio/fração/proprietário/valor e botão "Imprimir / guardar em PDF" (via impressão do browser — sem biblioteca de PDF nova). **2026-07-26**: (1) ícone de recibo passou a estar visível a qualquer membro com acesso financeiro na lista de movimentos e no mapa mensal, não só no menu de ações do admin — um condómino já não precisa de pedir ao administrador; (2) `movimento.pagadorNome`/`pagadorNif` (migração 0038, aplicada a produção) permitem que o recibo mostre o nome/NIF de quem pagou, quando diferente do proprietário registado — cenário comum de herança indivisa em que cada herdeiro paga a sua quota-parte e precisa do seu próprio recibo com o seu NIF pessoal para efeitos de dedução de mais-valias em IRS (art. 51.º CIRS), incluindo quotas extraordinárias para obras — pesquisa feita confirma que a orientação fiscal consistente exige "nome e NIF do herdeiro", não da herança, e que o recibo de quitação do condomínio é aceite também neste caso; (3) texto legal do rodapé corrigido — o condomínio não é, em regra, sujeito passivo de IVA e não emite fatura (art. 23.º, n.º 4 do CIRC), o recibo já é o documento correto. | — |
| Pagamentos | ✅ Implementado 2026-07-22 | Além do booleano `pago`, o movimento regista meio de pagamento (transferência/multibanco/numerário/cheque/outro), referência multibanco e data de liquidação (distinta da data do lançamento). Capturados ao criar um movimento já pago, ou ao marcar como pago um movimento pendente (`components/financas/marcar-pago-dialog.tsx`) — em ambos os casos opcionais, para não travar quem só quer registar rápido. Visíveis no recibo e na exportação CSV. Testado em runtime com conta descartável. | — |
| Reconciliação bancária | ✅ Implementado 2026-07-21 | Importação de extrato bancário CSV (`components/financas/importar-extrato-dialog.tsx`) com mapeamento manual de colunas (Data/Descrição/Valor único ou Débito+Crédito, delimitador e separador decimal configuráveis) — não assume o formato de nenhum banco específico. Conciliação manual entre linhas do extrato e movimentos pagos da conta corrente (`destino: 'geral'`), com sugestões automáticas por valor igual + data próxima (`lib/extrato.ts:sugerirCorrespondencias`), opção de ignorar linhas sem correspondência esperada (ex: taxas bancárias) e desfazer conciliações. Só abrange a conta corrente — o fundo de reserva fica de fora, por estar tipicamente noutra conta bancária. Testado em runtime com movimentos e extrato fictícios. | — |
| Relatórios financeiros | 🟡 Parcial | 3 cartões (receitas/despesas/saldo) mais o mapa de saldos por fração. **Atualizado 2026-07-23**: mapa mensal fração × mês já resolvido (ver linha própria abaixo). **Atualizado 2026-07-24**: execução orçamental por rubrica também resolvida (ver linha "Orçamento anual") — o que falta agora é especificamente o balanço patrimonial (Ativo/Passivo/Situação Líquida, ver linha própria). | P1 → P2 |
| Exportação Excel/PDF | 🟡 Parcial — CSV 2026-07-07, PDF 2026-07-21 | Botão "Exportar CSV" na lista de movimentos (abre corretamente no Excel, com BOM UTF-8 para acentos/€). **Feito 2026-07-21**: `/financas/relatorio` — relatório de movimentos imprimível (mesmo padrão do recibo: `window.print()`, sem biblioteca nova), com totais e lista completa, acessível pelo botão "Relatório (PDF)" na tab Movimentos. Verificado em runtime. **Não é um `.xlsx` real** — decisão deliberada de não introduzir a dependência `exceljs` sem necessidade confirmada; CSV cobre a prestação de contas com risco mínimo. | P3 (evoluir para `.xlsx` real, só se necessário) |
| Declarações de dívida | ✅ Implementado 2026-07-22 | Documento formal exigido em compra/venda de fração (art. 1424º-A CC, Lei n.º 8/2022 — achado LEGAL-01 da auditoria jurídica, `docs/audit/LEGAL_COMPLIANCE_AUDIT.md`). Página `/financas/declaracao-divida/[fracaoId]`, acessível pelo botão "Declaração" no separador "Dívidas por fração" — mostra encargo corrente (quota mensal atual, calculada a partir do orçamento mais recente via `lib/rateio.ts`) e lista de dívidas existentes (natureza, data de constituição/vencimento, valor), com total. Imprimível, mesmo padrão do recibo/balanço. Testado em runtime com conta descartável. | — |
| Mapas de saldos | ✅ Implementado 2026-07-07 | Ver dívidas por fração, acima — é a mesma funcionalidade. | — |
| Mapa mensal de quotizações (vista calendário fração × mês) | ✅ Implementado 2026-07-23 | Separador "Mapa mensal" em `/financas` (`getMapaMensalQuotas`, `components/financas/mapa-mensal-tab.tsx`) — grelha fração × mês (Jan–Dez) com total por linha, seletor de ano, célula colorida por estado (pago/parcial/pendente), layout inspirado no mapa "Quotas" de administrações externas (`Exemplo MBD.pdf`). Clicar numa célula abre os movimentos desse mês/fração com as mesmas ações já existentes (editar, marcar pago, eliminar). Exportável em CSV (mesmo layout fração × mês) e em PDF imprimível A4 (`/financas/mapa-mensal`). Inclui também `atualizarMovimento` — antes só era possível criar/eliminar/marcar como pago um movimento, não corrigir valor/categoria/descrição/data/destino já lançados. Testado em runtime com fração e movimentos fictícios. | — |
| Exercício financeiro e conta bancária como entidades (saldo inicial/final, fecho auditado, IBAN, tipo à ordem/prazo/caixa) | ✅ Implementado 2026-07-24 (Fase A.1), em produção desde 2026-07-24 | Novas tabelas `exercicio_financeiro`, `conta_financeira`, `saldo_inicial_conta`, ligadas a `movimento`/`extratoBancario` por FK composta `(id, condominioId)` para isolamento multi-tenant ao nível da BD. Fecho de exercício com resumo/avisos/bloqueios pré-visualizados (`app/actions/exercicios.ts:prepararFechoExercicio`), bloqueio central de qualquer escrita em `movimento` dentro de um exercício fechado (`lib/contas-financeiras.ts:garantirExercicioAberto`, guarda de regressão em `lib/protecao-exercicio-financeiro.test.ts`), transporte de saldo entre exercícios por revisão explícita (nunca silencioso), associação em massa de movimentos antigos sem exercício/conta com pré-visualização obrigatória. UI em `/financas` → separador "Exercícios e contas", com assistente guiado de primeira configuração. IBAN validado (checksum mod-97), normalizado e mascarado para quem não tem acesso de gestão. Testado com `pnpm test`/`pnpm test:db` (18 testes de integração) e manualmente em runtime (dev). **Promovido a produção em 2026-07-24**: migração `0024` aplicada à BD de produção com drift confirmado a zero e totais financeiros inalterados, código publicado, e verificado em produção que as páginas principais carregam sem regressões (ver `docs/CHECKLIST_TESTE_MANUAL.md`). Ver `docs/product/MBD_GEST_GAP_ANALYSIS.md` (G01, G02, secção 12 — Fase A.1). | — |

## 4. Ocorrências e manutenção

| Funcionalidade | Estado | Nota | Prioridade |
|---|---|---|---|
| Comunicação de avarias | ✅ Implementado | `app/(app)/ocorrencias/page.tsx` + `app/actions/ocorrencias.ts`. | — |
| Pedidos de intervenção | ✅ Implementado | Mesma funcionalidade acima cobre este caso de uso. | — |
| Fotos/anexos | ✅ Implementado 2026-07-09 | `ocorrencia.fotoUrl` via Vercel Blob (`app/actions/ocorrencias.ts`), incluindo eliminação em cascata do ficheiro. | — |
| Estado da ocorrência | ✅ Implementado | `aberta \| em_curso \| resolvida`, com validação de enum. | — |
| Prioridade | ✅ Implementado | `normal \| importante \| urgente`. | — |
| Fornecedores | 🟡 Entidade implementada 2026-07-22, ligação a despesas 2026-07-22, **atribuição a ocorrências 2026-07-26** | `/fornecedores` (nome, NIF, categoria, contacto, notas) — CRUD completo, mesmo padrão de `/fracoes`. Despesas em `/financas` podem ser associadas a um fornecedor (`movimento.fornecedorId`). Ocorrências também: `ocorrencia.fornecedorId`, seletor visível só ao admin/gestor em `/ocorrencias`, nome do fornecedor mostrado no cartão a todos os membros — testado em runtime e **em produção desde 2026-07-26** (migração `0033`, `db:check-drift` OK). Sem nenhuma ligação a despesas/orçamento a partir daqui (ver linhas abaixo). **Falta ainda**: suporte aos "três orçamentos" para obras extraordinárias (achado LEGAL-05 da auditoria jurídica). | P3 (resto) |
| Orçamentos (de obra) | ❌ Em falta | Sem suporte a anexar/comparar orçamentos de fornecedores para uma intervenção. | P2 |
| Aprovação de despesas | ❌ Em falta | Sem fluxo de aprovação (ex. despesas acima de X requerem aprovação em assembleia — Código Civil distingue despesas de conservação corrente das que exigem deliberação). | P2 |
| Histórico de intervenções | 🟡 Parcial | Existe `updatedAt` na ocorrência, mas sem histórico de estados (não se sabe quando passou de "aberta" a "em curso"). | P2 |
| Obras urgentes | ❌ Em falta | Sem distinção/fluxo próprio (juridicamente relevante — o administrador pode decidir obras urgentes sem esperar por assembleia, art. 1427º CC). | P2 |
| Obras aprovadas em assembleia | ❌ Em falta | Depende do módulo de Assembleias (secção 2). | P2 |

## 5. Comunicação

| Funcionalidade | Estado | Nota | Prioridade |
|---|---|---|---|
| Avisos aos condóminos | ✅ Implementado | `app/(app)/avisos/page.tsx`, com prioridade. | — |
| Notificações (push/email) | ✅ Implementado 2026-07-09 — email enviado quando um aviso `importante`/`urgente` é publicado (a todos os membros aprovados) e quando o estado de uma ocorrência é atualizado (a quem a reportou). Avisos `normal` não geram email, para não sobrecarregar a caixa de entrada. Sem notificação push (só email). | — |
| Mensagens internas (condómino ↔ admin) | ❌ Em falta | Sem qualquer canal de mensagem direta/privada. | P2 |
| Histórico de comunicações | ✅ **Resolvido 2026-07-26, em produção desde 2026-07-26** | Confirmação de leitura por membro (ver linha abaixo) mostra quem já viu cada aviso, além da listagem indefinida já existente. | — |
| Confirmação de leitura | ✅ **Resolvido 2026-07-26, em produção desde 2026-07-26** — botão "Confirmar leitura" em avisos e na convocatória de assembleia, registado em `confirmacao_leitura` (membro, data/hora, `unique(membroId, entidade, entidadeId)`); administrador vê contagem/lista de quem confirmou | — |
| Regras contra exposição de emails entre condóminos | ✅ Resolvido 2026-07-07 | Ver `SECURITY_AUDIT.md` S13. | — |

## 6. Documentos

| Funcionalidade | Estado | Nota | Prioridade |
|---|---|---|---|
| Upload seguro | ✅ Implementado 2026-07-09, **bucket público resolvido 2026-07-22** | Vercel Blob (`lib/storage.ts`), com validação de tipo/tamanho por finalidade (PDF/imagem, 8–15MB). Novos uploads passam a usar o store privado `gestcondo-ficheiros-privado` (`access: 'private'`), servidos só através de `app/api/ficheiros/route.ts`, que verifica sessão + `condominioId` antes de devolver o ficheiro — confirmado que o URL do Blob deixou de ser acessível diretamente (403) e que a rota exige sessão (401 sem cookie). **Confirmado 2026-07-22 em produção**: 0 ficheiros com `url` preenchido em `documento`/`ocorrencia`/`seguro`, e 0 blobs no store antigo `gestcondo-ficheiros-publico` — não há nenhum ficheiro real por migrar; o store antigo fica apenas como fallback morto na rota, sem uso. **Nota operacional importante:** o Blob store da Vercel tem de ser criado com o modo de acesso (Public/Private) já decidido — não pode ser alterado depois de criado, só criando um store novo. | — |
| Classificação por tipo | ✅ Implementado | `ata \| regulamento \| orcamento \| outro`. | — |
| Controlo de permissões por documento | ❌ Em falta | Hoje é tudo-ou-nada (qualquer aprovado vê tudo). Não há documentos privados/confidenciais (ex. um orçamento de fornecedor em negociação). | P2 |
| Versionamento | ❌ Em falta | Substituir um documento perde a versão anterior. | P2 |
| Atas / Faturas / Recibos / Contratos / Apólices / Orçamentos / Relatórios | 🟡 Só como categoria de texto | Sem tratamento diferenciado nem campos próprios (ex. uma fatura devia ter fornecedor, valor, NIF). | P2 |
| Exportação e arquivo | ❌ Em falta | — | P2 |

## 7. Auditoria (funcionalidade de produto, distinta da auditoria de segurança)

| Funcionalidade | Estado | Nota | Prioridade |
|---|---|---|---|
| Registo de ações importantes | ✅ Implementado 2026-07-06 | `audit_log` (ver `SECURITY_AUDIT.md` S17). | — |
| Quem criou/alterou/apagou dados | ✅ Implementado 2026-07-06 | Idem — ator, ação, entidade, timestamp. | — |
| Histórico de alterações | 🟡 Parcial | O `audit_log` regista *que* algo foi alterado e por quem, mas não um diff antes/depois do conteúdo (só um resumo em texto livre opcional). | P2 |
| Logs consultáveis por admin autorizado | ✅ Implementado 2026-07-06 | Página `/auditoria`, acessível a admin/gestor/auditor. | — |
| Proteção contra alteração indevida de atas/documentos/deliberações | ✅ Implementado 2026-07-09 para atas/deliberações | Uma ata com `estado: 'aprovada'` bloqueia qualquer escrita nas tabelas associadas (secção 2), verificado em runtime em 2026-07-21. Documentos carregados continuam sem versionamento nem imutabilidade própria (secção 6). | P2 (versionamento de documentos) |

## 8. Perfis de utilizador pedidos vs. existentes

**Atualizado 2026-07-06:** os 7 perfis foram modelados (`lib/perfis.ts`) e ligados às permissões reais em todas as server actions e páginas.

| Perfil pedido | Existe? |
|---|---|
| Super Admin (operador da plataforma) | 🟡 Flag `user.superAdmin` existe e dá poderes de gestão/consulta em qualquer condomínio a que a pessoa também pertença como `membro`. **Onboarding para novos condomínios resolvido 2026-07-22** (ver acima); falta ainda um painel cross-condomínio dedicado — ver Fase 5 do `ROADMAP.md`. |
| Empresa de administração (gere vários condomínios) | 🟡 `perfil: 'gestor'` tem exatamente os mesmos poderes que `admin` dentro de um condomínio, e a mesma pessoa pode ter uma linha `membro` com `perfil: 'gestor'` em vários condomínios em simultâneo (o schema já suporta isto, e já é possível criar/juntar-se a mais do que um condomínio — ver acima). Falta um seletor de "condomínio ativo" na sessão para uma conta que faça parte de vários — hoje `getMembroAtual()` só considera a primeira linha `membro` da conta. |
| Administrador do condomínio | ✅ `perfil: 'admin'`, com âmbito por condomínio (`membro.condominioId`). |
| Condómino proprietário | ✅ `perfil: 'condomino'` — vê dados financeiros/patrimoniais (`PERFIS_ACESSO_FINANCEIRO`). |
| Inquilino | ✅ `perfil: 'inquilino'` — vê avisos/documentos/ocorrências, mas **não** finanças nem frações (nem no dashboard nem nas páginas dedicadas, que devolvem 404 se acedidas diretamente). |
| Fornecedor externo | 🟡 `perfil: 'fornecedor'` existe com o mesmo acesso mínimo que inquilino. Falta o fluxo de atribuição de ocorrências/orçamentos a um fornecedor específico (P2, ver secção 4). |
| Auditor/consulta (leitura apenas) | ✅ `perfil: 'auditor'` — vê tudo o que admin/gestor veem (incluindo a lista de condóminos), mas `requireMembroComEscrita`/`podeEscrever` bloqueia qualquer escrita (criar aviso, reportar ocorrência, aprovar condómino, etc.). |

## 9. Resumo de prioridades P0/P1 (o que bloqueia mesmo o MVP)

1. ~~**P0** — Entidade `condominio` + isolamento multi-tenant~~ **Resolvido 2026-07-06** (schema e queries) **+ 2026-07-22** (onboarding/convite para um segundo condomínio).
2. ~~**P0/P1** — Log de auditoria mínimo + soft-delete em dados financeiros~~ **Resolvido 2026-07-06.**
3. ~~**P1** — Módulo de Assembleias (convocatória → ordem de trabalhos → presenças/procurações → quórum → votação → ata).~~ **Resolvido 2026-07-09** (núcleo P1); confirmação de leitura e anexos à ata resolvidos 2026-07-26.
4. ~~**P1** — Gestão financeira formal: orçamento, dívida por fração, recibos, exportação PDF/Excel.~~ **Resolvido 2026-07-07/21** — orçamento, dívida por fração, recibos, rateio automático por permilagem (com isenção de elevador), juros de mora, reconciliação bancária, CSV e relatório PDF. A exportação `.xlsx` real continua por fazer, mas está classificada **P3** na secção 3, não P1.
5. ~~**P1** — Upload de ficheiros (documentos, faturas, fotos de ocorrências) com controlo de acesso.~~ **Resolvido 2026-07-09** via Vercel Blob (`lib/storage.ts`); controlo de acesso ao nível da página, não por ficheiro (ver secção 6).
6. ~~**P1** — Envio de email: reset de password/verificação, convocatórias, avisos importantes, atualização de ocorrências.~~ **Resolvido 2026-07-09.**
7. ~~**P1** — Distinção proprietário/inquilino e correção da exposição de contactos.~~ **Resolvido 2026-07-07** (`membro.fracaoId`, `SECURITY_AUDIT.md` S13); compropriedade e NIF do proprietário **resolvidos 2026-07-21** (ver secção 1).
8. ~~**P1** — Seguro obrigatório e fundo de reserva como entidades próprias, não texto livre/implícito.~~ **Resolvido 2026-07-08.**

Tudo o que está classificado P2/P3 é razoável adiar para depois de um MVP fechado, mas **nenhum item P1 pode ficar de fora de uma versão vendável a uma administração de condomínios real em Portugal** — a ausência de Assembleias/Atas, em particular, torna a aplicação inadequada ao seu propósito declarado, por mais polida que a parte de finanças/avisos já esteja.
